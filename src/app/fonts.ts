import { Inter, Satisfy } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const SatisfyQo = Satisfy({ subsets: ["latin"], weight: ["400"] });

export default inter;
